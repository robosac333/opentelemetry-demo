pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'deployment', url: 'https://github.com/robosac333/opentelemetry-demo.git'
            }
        }
    }
}
